# Phase 6 Track B — Walk-the-94 Retrospective

**Walk dates:** 2026-05-23 — TBD
**Curator:** kun.lu
**Baseline:** open=94, merged=0, rejected=0

> Originally framed as "walk the 31." After Track C's backfill of Ch.1-5 (necessary to populate `person_relations` from 0 → 28), the queue grew to 94 open mcs — one per chapter-mention of a person who had not auto-merged. Most are quick name-match accepts; see knowledge/log.md's Track C entry for the structural explanation.

## Disposition summary

| Disposition | Count |
|---|---|
| Accepted | _ |
| Edit-accepted | _ |
| Rejected | _ |
| Split | _ |
| Deferred (resolved before close) | _ |

**Pre-walk snapshot:** `data/changjuan.sqlite.phase6-walk-bak` (rollback by `cp` if needed).

## Time

- Total elapsed: _ minutes / hours
- Mean seconds per candidate: _

## What worked

- _

## Friction observed (ranked, most-painful first)

For each item: what happened, why it slowed the walk, whether fixed in-budget or deferred.

1. **A-side fields rendered as '-'.** The merge-candidates review screen showed the candidate column entirely blank because `_load_person` only queried the `persons` table, while `candidate_a_id` typically references `candidate_persons` (Phase 5.1 dual-table reality). Phase 5 UI never picked up the Phase 5.1 read-side. *Status:* fixed in-budget (see Fix #1).
2. **Evidence column shows "(no citation linked to candidate)" for every candidate.** Same Phase 5.1 read-side gap: `render_left` queried `entity_citations` which only holds canonical-side entries. Candidate rows carry `chunk_id` + `quote` directly. *Status:* fixed in-budget (see Fix #2).

## Friction-fix budget usage

| # | Fix | Commit | LOC | Budget used |
|---|---|---|---|---|
| 1 | `_load_person` falls back to `candidate_persons` when the id isn't in `persons` (mirrors `_load_reject_payload` dual-table pattern). | `3e8f65e` | ~13 | 1/3 |
| 2 | `render_left` reads `candidate_persons.chunk_id` + `quote` directly and looks up the chunk text from corpus, falling back to `entity_citations` only for the persons-side escape-hatch. | _pending_ | ~25 | 2/3 |
| 3 | _ | _ | _ | 3/3 |

## Recommended next moves (Phase 7 input)

- _
